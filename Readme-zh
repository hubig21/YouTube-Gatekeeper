# 🔍 YouTube 頻道過濾器 — 使用說明

> 版本：v1.4.0-rc17 · Tampermonkey / Violentmonkey 腳本

---

## 目錄

1. [快速開始](#快速開始)
2. [介面概覽](#介面概覽)
3. [功能說明](#功能說明)
   - [隱藏名單](#-隱藏名單)
   - [白名單](#-白名單)
   - [影片黑名單](#-影片黑名單)
   - [關鍵字過濾](#-關鍵字過濾)
   - [設定](#️-設定)
   - [統計 / 歷史](#-統計--歷史)
   - [匯出入](#-匯出入)
4. [進階功能詳解](#進階功能詳解)
   - [啟用過濾範圍（高級）](#啟用過濾範圍高級)
   - [Regex 模式](#regex-模式)
   - [兒童模式設定技巧](#兒童模式設定技巧)
5. [快捷操作](#快捷操作)
6. [密碼鎖定](#密碼鎖定)
7. [注意事項](#注意事項)

---

## 快速開始

1. 安裝 [Tampermonkey](https://www.tampermonkey.net/) 或 Violentmonkey 瀏覽器擴充
2. 安裝本腳本
3. 開啟 YouTube，頂部導覽列會出現 **🔍** 按鈕
4. 左鍵點擊 🔍 開啟管理面板，即可開始使用

---

## 介面概覽

管理面板包含以下頁籤：

| 頁籤 | 說明 |
|------|------|
| 🚫 隱藏名單 | 管理要隱藏的頻道 |
| ⭐ 白名單 | 管理信任的頻道（可搭配白名單獨享模式） |
| 🎬 影片黑名單 | 封鎖特定影片 ID |
| 🔤 關鍵字 | 依標題 / 描述過濾影片 |
| ⚙️ 設定 | 全域設定、各類過濾條件 |
| 📊 統計 | 查看過濾統計 |
| 📋 歷史 | 查看被過濾的影片記錄 |
| 💾 匯出入 | 備份與還原設定 |
| 🎯 過濾範圍 | 指定各規則生效的頁面（需先在設定中啟用） |

---

## 功能說明

### 🚫 隱藏名單

新增你不想看到的頻道，該頻道在首頁、訂閱頁、搜尋結果等列表頁的卡片將被自動隱藏。

**新增方式：**
- **手動輸入**：在面板表單填入頻道名稱（必填）及 `@handle`（選填）後按新增
- **快捷按鈕**：滑鼠懸停影片卡片時，底部滑出操作列，點擊 🚫 即可即時封鎖
- **三點選單**：若設定為「三點選單」模式，點擊影片卡片的 ⋮ 選單，選擇「YtFC 隱藏此頻道」

**標籤功能：** 每個頻道可設定標籤，方便分類管理（例如「廣告帳號」、「農場頻道」）。

---

### ⭐ 白名單

加入你信任或想保護的頻道。白名單頻道永遠不受任何過濾規則影響，不會被隱藏名單、關鍵字等規則誤殺。

**白名單獨享模式：** 在 ⚙️ 設定中開啟「只顯示白名單頻道」，此時除白名單內的頻道外，其他所有頻道均會被隱藏。適合家長管控或極簡模式。

---

### 🎬 影片黑名單

封鎖特定影片 ID，即使不封鎖該頻道也能擋掉單支影片。常見用途：遮蔽特定爭議影片、屏蔽不感興趣的單集。

- 支援輸入影片 ID（11 字元）或完整 YouTube URL
- 加入後**永遠強制阻擋**，不受過濾範圍矩陣控制
- 直接透過影片 URL 進入時，也會顯示阻擋畫面（需在設定中啟用「強制阻擋」）

---

### 🔤 關鍵字過濾

依影片標題或描述中的關鍵字過濾。關鍵字以**群組**為單位管理，每個群組可個別啟用／停用。

**建立群組步驟：**
1. 點擊「＋ 新增群組」輸入群組名稱
2. 選擇套用範圍：標題、描述，或兩者皆套用
3. 在群組內新增關鍵字
4. 確認群組右側開關為「啟用」狀態

**群組優先順序：** 可拖動調整群組順序，上方群組優先判斷。

---

### ⚙️ 設定

| 設定項目 | 說明 |
|----------|------|
| 顯示操作通知（Toast） | 操作後是否彈出提示訊息 |
| 快捷操作樣式 | 選擇「快捷按鈕」（hover bar）或「三點選單」（融入 YouTube ⋮ 選單） |
| 只顯示白名單頻道 | 白名單獨享模式 |
| 影片時長 | 過濾過短或過長的影片（秒） |
| 直播 / 首播 | 隱藏直播中或即將首播的影片 |
| 會員影片 | 隱藏會員專屬或會員搶先看影片 |
| 觀看數 | 依觀看數過濾（低於或高於設定值） |
| 上傳日期 | 隱藏太舊或太新的影片 |
| 語言過濾 | 過濾或只顯示特定語言的影片 |
| 頻道公告 | 隱藏頻道公告貼文 |
| 募款活動 | 隱藏募款活動標示的影片 |
| 強制阻擋（直接網址訪問） | 阻擋直接輸入 URL 訪問被封鎖頻道／影片 |
| 啟用過濾範圍（高級） | 開啟後顯示「🎯 過濾範圍」頁籤 |
| 密碼鎖定 | 設定密碼保護管理面板 |

---

### 📊 統計 / 歷史

- **統計**：查看各規則的觸發次數、頻道封鎖排行
- **歷史**：查看最近被過濾的影片清單（含觸發原因）

---

### 💾 匯出入

支援完整備份與單項匯出，格式涵蓋 CSV（隱藏名單、白名單、關鍵字）及 JSON（所有設定、影片黑名單）。

**備份建議：** 在設定中開啟「自動備份」，定期自動匯出完整設定至下載資料夾。

---

## 進階功能詳解

### 啟用過濾範圍（高級）

「過濾範圍」讓你針對每種過濾規則，指定在哪些 YouTube 頁面類型生效。適合需要精細控制的進階使用者。

#### 啟用步驟

1. 進入 ⚙️ **設定** 頁籤
2. 滾動至最底部，勾選「**啟用過濾範圍 (高級)**」
3. 點擊「💾 儲存設定」
4. 頁籤列出現「🎯 **過濾範圍**」，點擊進入

#### 矩陣介面說明

介面呈現 **11 種規則 × 7 種頁面** 的矩陣：

**頁面類型：**

| 代碼 | 說明 |
|------|------|
| 🏠 首頁 | youtube.com 首頁 |
| 📨 訂閱 | 訂閱頁面 |
| 🔍 搜尋 | 搜尋結果頁 |
| 🎬 影片頁 | 觀看影片時的側邊推薦 |
| 📺 頻道頁 | 進入特定頻道時 |
| 🔥 熱門 | 熱門影片頁面 |
| 📄 其他 | 其他頁面 |

**規則類型：**
頻道隱藏名單、影片黑名單、關鍵字、會員影片、直播／首播、募款活動、上傳日期、觀看數、語言、影片時長、頻道公告。

#### 操作方式

- **點擊格子**：切換該規則在該頁面的啟用狀態（綠色 = 啟用、灰色 = 停用）
- **行末「✓ 全部啟用」**：該規則在所有頁面全開
- **行末「✕ 全部關閉」**：該規則在所有頁面全關
- **底部「全部重設為啟用」**：恢復預設（所有規則在所有頁面生效）
- **當前頁面**：以特殊樣式標示，方便即時對照

#### 應用範例

**範例 1｜允許在頻道頁看到隱藏名單頻道的影片**
> 取消勾選「頻道隱藏名單」× 「頻道頁」
> → 瀏覽封鎖頻道的頻道頁時，仍可正常看到影片；但在首頁等列表頁仍會被隱藏

**範例 2｜只在首頁過濾關鍵字**
> 對「關鍵字」規則，只保留「首頁」勾選，其餘頁面取消
> → 搜尋特定標題時不受關鍵字過濾影響

**範例 3｜訂閱頁不過濾直播**
> 取消勾選「直播 / 首播」× 「訂閱」
> → 訂閱的頻道直播仍會出現在訂閱頁，但首頁不顯示

---

### Regex 模式

關鍵字群組支援 JavaScript 正規表示式（Regular Expression），讓你以更彈性的方式比對影片標題或描述。

#### 啟用方式

在關鍵字群組內新增關鍵字時，勾選「**Regex**」欄位即可為該關鍵字啟用正規表示式模式。

#### 使用規則

- **不需要**加 `/` 斜線或 `/i` 旗標，直接輸入 pattern
- 預設為**不區分大小寫**比對
- 反斜線（`\`）在介面中直接輸入即可，不需要額外跳脫

#### 常用範例

| Pattern | 說明 |
|---------|------|
| `(廣告\|業配\|贊助)` | 標題含「廣告」、「業配」或「贊助」任一 |
| `(股票\|投資\|理財)` | 標題提到任一財經相關詞彙 |
| `EP\.?\d+` | 集數格式：EP1、EP.1、EP 1 等 |
| `^(?!.*官方).*$` | 標題**不含**「官方」的影片（負向先行斷言） |
| `\d{4}年` | 標題含四位數字年份（如「2024年」） |
| `(LIVE\|直播\|STREAM)` | 各種直播標示 |

#### 注意事項

- Regex 只在**該關鍵字**啟用，同一群組內其他關鍵字可繼續使用純文字
- 若 pattern 格式錯誤（如括號不匹配），該關鍵字將自動退回純文字模式
- 過於複雜的 pattern 可能影響效能，建議優先使用 `(a|b|c)` 形式而非 lookahead

---

### 兒童模式設定技巧

透過組合現有功能，可以建立一個「白名單專享 + 危險內容自動過濾」的兒童友善瀏覽環境。

> ⚠️ **重要警語：** 本腳本僅過濾首頁、搜尋、推薦等列表頁，無法阻止直接輸入影片 URL，也無法偵測影片的實際內容。**強烈建議搭配 YouTube Kids、家長監護軟體（Google Family Link 等）或路由器層級的 DNS 過濾同時使用。**

#### 完整設定步驟

**第 1 步｜建立白名單**
進入 ⭐ **白名單** 頁籤，將孩子可以觀看的頻道逐一加入。
例如：@PBSKids、@SesameStreet、@CocomelonOfficial

**第 2 步｜啟用白名單獨享模式**
進入 ⚙️ **設定**，開啟頂部的「⭐ 只顯示白名單頻道」。
啟用後，除白名單內的頻道外，所有其他頻道均會被隱藏。

**第 3 步｜封鎖直播**
在設定中開啟「📡 直播 / 首播」區塊，同時勾選：
- 隱藏直播中
- 隱藏即將首播 / 即將直播（等候中）

直播聊天室是常見的風險來源，建議全數封鎖。

**第 4 步｜建立危險內容關鍵字群組**
進入 🔤 **關鍵字** 頁籤，建立名為「危險內容」的群組，套用範圍選擇「標題」：

加入以下關鍵字（可依需求調整）：
```
暴力、血腥、恐怖、成人、賭博、槍、毒品
```

若要更有效率，可開啟 Regex 模式，輸入：
```
(暴力|血腥|恐怖|成人|賭博|18禁|限制級)
```

**第 5 步（建議）｜限制影片日期**
在設定中開啟「📅 上傳日期」，設定「隱藏 30 天前上傳的影片」。
避免演算法推送過時的爭議性內容。

**第 6 步（建議）｜語言過濾**
在設定中開啟「🌐 語言過濾」，選擇「僅顯示指定語言」，只勾選孩子的母語。
可有效過濾大量未知語言的內容。

**第 7 步（建議）｜搭配密碼鎖定**
在設定中設定「密碼鎖定」，避免孩子自行修改過濾設定。

#### 搭配過濾範圍的進階設定

啟用「過濾範圍（高級）」後，可進一步細化兒童模式：

- **首頁、訂閱、搜尋**：所有規則全開
- **影片頁側邊欄**：開啟所有規則，避免側邊推薦出現不當內容
- **頻道頁**：可放鬆「頻道隱藏名單」規則，讓家長進入特定頻道做確認

---

## 快捷操作

| 操作 | 說明 |
|------|------|
| 左鍵點擊頂部 🔍 | 開啟管理面板 |
| 右鍵點擊頂部 🔍 | 切換過濾器開啟 / 暫停 |
| 滑鼠懸停影片卡片 | 底部滑出快捷操作列（快捷按鈕模式） |
| 點擊影片 ⋮ 選單 | 出現 YtFC 選項（三點選單模式） |
| Tampermonkey 選單 | 提供「開啟面板」「切換過濾器」選項 |

**兩種快捷操作模式（在設定中切換）：**
- **快捷按鈕**：懸停影片時，底部滑出「🚫 隱藏此頻道」「⭐ 加入白名單」「🎬 隱藏此影片」三個按鈕
- **三點選單**：上述三個選項融入 YouTube 原生 ⋮ 選單，視覺更一致

---

## 密碼鎖定

在 ⚙️ 設定的「密碼鎖定」區塊中啟用：

- 設定後，每次開啟管理面板都需輸入密碼
- 未解鎖時，所有頁籤和全域開關均無法操作
- 關閉面板後自動重新鎖定
- 「Blocker 阻擋畫面」上的移除按鈕也受密碼保護（獨立驗證，不繼承面板解鎖狀態）

---

## 注意事項

- 腳本僅過濾**列表頁卡片**，無法過濾影片播放頁的實際內容
- Shorts 不在過濾範圍內，建議搭配專用的 Shorts 過濾腳本
- YouTube 頻道頁不含 UC ID 時，比對主要依賴 `@handle` 和頻道名稱
- 若封鎖頻道的卡片仍出現，可嘗試重新整理頁面或在管理面板按「儲存設定」
- 設定儲存於瀏覽器 localStorage，清除瀏覽器資料前請先匯出備份
# YouTube 頻道過濾器 — 使用說明

> v1.1.0 ｜ Tampermonkey userscript ｜ 繁中 & English

<br>

## 🔗 快速導覽

- [安裝](#安裝)
- [語系切換](#語系切換)
- [🚫 隱藏名單 Blocklist](#-隱藏名單-blocklist)
- [⭐ 白名單 Whitelist](#-白名單-whitelist)
- [🔤 關鍵字 Keywords](#-關鍵字-keywords)
- [⚙️ 設定 Settings（過濾條件）](#️-設定-settings過濾條件)
- [📊 統計 Stats & 📋 歷史 History](#-統計-stats--歷史-history)
- [💾 匯出入 Import / Export](#-匯出入-import--export)
- [🏷️ 標籤系統](#️-標籤系統)
- [🔍 Regex 模式完整指南](#-regex-模式完整指南)
- [🛡️ 兒童保護設定範例](#️-兒童保護設定範例)
- [⌨️ 快捷操作](#️-快捷操作)
- [常見問題](#常見問題)

---

## 安裝

1. 瀏覽器安裝 **Tampermonkey** 擴充功能（Chrome / Firefox / Edge 皆可）
2. 開啟 userscript 檔案：`youtube-channel-filter-v1.1.0.user.js`
3. Tampermonkey 自動提示安裝 → 確認即可
4. 打開任何 YouTube 頁面，右下角應看到 🔍 浮動按鈕

### 驗證版本

開啟面板（點 🔍 或 Tampermonkey 選單「⚙️ 開啟管理面板」），**面板右上角應顯示 `v1.1.0`** 字樣。

---

## 語系切換

介面預設跟隨系統語言（繁中 / 英文）。切換方式：

1. 點擊 🔍 開啟管理面板
2. 到「⚙️ 設定」Tab
3. **最頂部**「⚙️ 全域設定」區塊 → 找「🌐 介面語言」下拉選單
4. 選擇「繁體中文」或「English」→ **立即生效**（面板與所有卡片按鈕皆會更新）

語言設定儲存在 `ytf_lang`，跨瀏覽器分頁同步。

---

## 🚫 隱藏名單 Blocklist

所有加入此清單的頻道，其所有影片將從列表頁（首頁、搜尋、推薦、訂閱等）**完全隱藏**。

### 新增頻道

#### 方法 A：從影片卡片快速加入
1. 滑鼠 hover 任何影片卡片
2. 卡片底部滑出「🚫 隱藏此頻道」按鈕 → 點擊
3. 該頻道所有影片立即消失

#### 方法 B：從面板手動加入
1. 開啟面板 → 「🚫 隱藏名單」Tab
2. 點「＋ 新增」按鈕
3. 填入頻道名稱或 @handle，可選擇勾選「⌥R.* Regex 模式」
4. 點「確認」

### 管理條目

| 按鈕 | 說明 |
|------|------|
| ✏️ | 編輯頻道名稱 / ID / Regex 模式 / 標籤 |
| ✕ | 移除（雙次確認避免誤刪） |
| 🏷 | 打開該條目的標籤管理面板 |
| 🗑 清空全部 | 清空整個隱藏名單（有確認對話框） |

### 搜尋

上方的**搜尋欄**可即時搜尋頻道名稱或 @handle；**標籤篩選列**可按標籤過濾。

---

## ⭐ 白名單 Whitelist

所有加入此清單的頻道將**永遠顯示**（不受任何過濾規則影響）。

### 用途
- 確保重要頻道不會被關鍵字或其他過濾條件誤殺
- 配合「⭐ 只顯示白名單頻道」設定，打造僅限信任頻道的瀏覽模式（如兒童模式）
- **已加入白名單的頻道不會顯示「🚫 隱藏」快捷按鈕**（減少誤操作）

### 新增 / 管理

操作方式與隱藏名單相同（可從卡片快捷按鈕 ⭐ 或從面板手動加入）。

白名單條目也支援：
- **標籤管理**（🏷 按鈕）
- **編輯**（✏️ 按鈕）
- **標籤篩選列**（頂部 chip 列）
- **CSV 匯出含 tags 欄位**

白名單與隱藏名單的**標籤池完全獨立**，即使同名也各算各的。

---

## 🔤 關鍵字 Keywords

可建立多個「關鍵字群組」，每個群組包含多個關鍵字，影片標題或描述命中任一關鍵字即會被隱藏。

### 建立群組

1. 點「＋ 新增群組」
2. 填入群組名稱（如「政治」、「業配」、「危險內容」）
3. 選擇**套用範圍**：「標題」、「描述」（至少勾選一個）
4. 點「建立」

### 加入關鍵字

1. 在群組內點「＋ 關鍵字」
2. 輸入關鍵字或 Regex 模式
3. 勾選「Regex」checkbox 則啟用正規表達式模式
4. 點「加入」

### 管理群組

| 按鈕 | 說明 |
|------|------|
| 🔴 / ⚪ | 啟用 / 停用該群組（不刪資料） |
| ↑ ↓ | 調整群組優先順序 |
| ✏️ | 編輯群組名稱 / 套用範圍 |
| 🗑 刪除群組 | 刪除整個群組（有確認） |

### 搜尋群組 / 關鍵字

群組列表上方的**搜尋框**可即時過濾：
- 輸入「業配」→ 只顯示群組名或關鍵字含「業配」的群組
- 命中的關鍵字 chip 會以**金色螢光**高亮，群組名命中則**金色文字**

---

## ⚙️ 設定 Settings（過濾條件）

設定頁分成多個區塊，由上至下：

### 💾 儲存設定（置頂）
按下此按鈕才會儲存你做的所有設定變更。

### ⚙️ 全域設定
- **🌐 介面語言**：繁體中文 / English
- **顯示操作通知 (Toast)**：關閉後 toast 不出現，但 log 仍記錄
- **顯示統計 & 歷史 Tab**：關閉後 UI 不顯示這兩個 Tab，資料仍追蹤

### ⭐ 白名單獨享模式
開啟後**只顯示白名單頻道**，其他所有頻道被隱藏。這是兒童保護設定的核心開關。

### ⏱ 影片時長
- 最短 / 最長時長（秒）
- 範例：`60 / 600` → 只顯示 1 到 10 分鐘的影片

### 📡 直播 / 首播
- 隱藏直播中影片
- 隱藏即將首播影片

### 👑 會員專屬
隱藏所有會員才能看的影片

### 📅 上傳日期
- 支援語義範圍：「N 天內」、「N 週內」、「N 個月內」、「N 年內」
- 預設「30 天內」

### 👁 觀看數
- 最低 / 最高觀看數
- 直接輸入數字（10000）或用 K/M 縮寫（10K = 10,000, 1M = 1,000,000）
- 直播觀看人數**不計入**

### 🌐 語言過濾
- **模式**：阻擋特定語言 / 僅允許特定語言
- **常用語言勾選**：日文 / 韓文 / 中文 / 英文 / 俄文 / 阿拉伯文 / 西班牙文 / 法文 / 德文 / 葡萄牙文 / 印地文 / 泰文 / 越南文 / 印尼文 / 土耳其文 / 義大利文
- **自訂代碼**：逗號分隔的 BCP-47 代碼（如 `nl, pl, sv`）
- **偵測原理**：標題字元集分析。拉丁語系（越南文、法文等）因字元重疊偵測準確度較低，建議搭配關鍵字過濾

---

## 📊 統計 Stats & 📋 歷史 History

### 📊 統計
- **關鍵字排行**：哪個關鍵字最常觸發隱藏
- **頻道排行**：哪個頻道最常被隱藏（顯示名稱便於辨識）
- 兩欄並排顯示，一目了然

### 📋 歷史
- 最近 **500 筆**被隱藏的影片記錄
- 包含：影片標題、頻道名稱、頻道 ID、觸發原因、隱藏時間
- 支援搜尋與匯出 CSV

---

## 💾 匯出入 Import / Export

### 匯出全部設定（JSON）
打包**所有**隱藏名單、白名單、關鍵字、過濾設定為 JSON 檔案。

### 分別匯出（CSV）
- 隱藏名單 CSV（含標籤欄）
- 白名單 CSV（含標籤欄）
- 關鍵字 CSV（含群組名、範圍、Regex 標記）
- 統計資料 JSON
- 歷史記錄 CSV（觸發原因會**本地化**為描述文字）

所有 CSV 都包含 **UTF-8 BOM**，Windows Excel 可直接開啟不亂碼。

### 匯入設定
1. 選擇匯入模式：**覆蓋** / **合併**
2. 選擇檔案：JSON（完整備份）或 CSV（單項）

### 自動備份
- 開啟「自動備份」→ 每 N 小時自動下載一次備份 JSON
- 可自訂檔名前綴（預設 `yt-filter-backup-`）

### 分享清單
一鍵產生包含選定清單（隱藏 / 白名單 / 關鍵字 / 全部）的 URL 連結，他人點擊即可在他們的腳本中一鍵匯入。**不傳送到任何伺服器**（資料編碼在 URL 本身）。

---

## 🏷️ 標籤系統

### 為什麼用標籤？

當隱藏名單或白名單有幾百個頻道時，用**標籤分組**方便管理：
- 「政治」「業配」「搬運」分類
- 「可信」「待觀察」「兒童適用」等自訂分類
- 離線處理匯出的 CSV 時可用 Tags 欄分類整理

### 新增標籤

#### 方法 A：透過 🏷 快速面板
1. hover 頻道條目 → 點 🏷
2. 面板展開，分兩區塊：
   - **📝 新增標籤** — 輸入新標籤 + 按 Enter 或點 ＋
   - **📌 所有標籤（✓已擁有、點擊切換）** — 系統所有標籤的 chip 列，**點擊即切換狀態**

#### 方法 B：編輯表單
點 ✏️ 打開編輯表單，「tags」欄位支援**逗號分隔**輸入，同時下方有「📌 所有標籤」chip 列可點擊切換。

### 篩選

每個清單頂部有 **標籤篩選列**：
- `全部 (200)` → 顯示所有條目
- `政治 (15)` → 只顯示含「政治」標籤的條目

### 重要規則

| 項目 | 說明 |
|------|------|
| 標籤獨立性 | 隱藏名單標籤 vs 白名單標籤**各自獨立**，即使同名也分別計算 |
| 即時同步 | 新增 / 移除標籤後所有相關 UI 立即刷新 |
| 空篩選自動重置 | 若篩選的標籤被完全移除，清單自動回到「全部」 |

---

## 🔍 Regex 模式完整指南

### 何時使用 Regex

在「頻道隱藏 / 白名單」條目或「關鍵字群組」內勾選 **⌥R.* Regex 模式** 後啟用。適合：
- 比對特定字首 / 字尾
- 批次命名規則（EP1 / EP.1 / EP 1 等）
- 避免部分匹配（如只要「NASA」不要「NASA-related」）

**預設不分大小寫**。

### 常用語法

| 語法 | 說明 |
|------|------|
| `^abc` | 以 abc 開頭 |
| `abc$` | 以 abc 結尾 |
| `a\|b` | 符合 a 或 b |
| `\d+` | 一個或多個數字 |
| `\b` | 單詞邊界（避免部分匹配） |
| `.*` | 任意字元（零或多個） |
| `[0-9]` | 字元範圍 |

### 實用範例

| Pattern | 比對 |
|---------|------|
| `^\[業配\]` | 所有標題以「[業配]」開頭的影片 |
| `\bNASA\b` | 獨立出現 NASA（避開「NASA-related」） |
| `(股票\|投資\|理財)` | 提到股票、投資、理財任一關鍵字 |
| `EP\.?\d+` | 匹配 EP1 / EP.1 / EP 1 等集數格式 |
| `^(?!.*官方).*$` | 標題**不**包含「官方」的所有影片（負向先行斷言） |

### 提示

不需要加 `/` 或 `/i` 標記，**直接輸入 pattern** 即可。反斜線跳脫由 UI 自動處理。

---

## 🛡️ 兒童保護設定範例

利用腳本現有功能，打造「白名單專享 + 危險內容自動過濾」的兒童友善瀏覽環境：

### 6 步驟

**第 1 步**｜**白名單**：到 ⭐ 白名單 Tab 加入孩子可看的頻道（例如 `@PBSKids`、`@SesameStreet`、`@CocomelonOfficial`）

**第 2 步**｜**白名單獨享**：到 ⚙️ 設定 最頂部，勾選「⭐ 只顯示白名單頻道」 — 其他所有頻道都會被隱藏

**第 3 步**｜**禁直播**：勾選「📡 直播 / 首播」區塊內的「隱藏直播中」和「隱藏即將首播」 — 避免直播聊天室風險

**第 4 步**｜**關鍵字黑名單**：到 🔤 關鍵字 建立「危險內容」群組，加入如「暴力、血腥、恐怖、成人、賭博」等字詞並啟用（可用 Regex：`(暴力|血腥|恐怖|成人)`）

**第 5 步（可選）**｜**限制日期**：到 📅 上傳日期 設定「隱藏 30 天前上傳的影片」 — 避免演算法推送陳年爭議內容

**第 6 步（可選）**｜**語言過濾**：到 🌐 語言過濾 選擇「僅顯示指定語言」並只勾選孩子的母語

### ⚠️ 重要警語

此腳本僅過濾首頁 / 搜尋 / 推薦等列表頁，**無法阻止**：
- 直接輸入影片 URL
- 偵測影片實際內容（只看 metadata）

**強烈建議搭配**：
- YouTube Kids 應用程式
- 家長監護軟體（Google Family Link 等）
- 路由器層級的 DNS 過濾（如 CleanBrowsing、NextDNS）

---

## ⌨️ 快捷操作

| 操作 | 行為 |
|------|------|
| 頂欄 🔍 按鈕 **左鍵** | 開啟管理面板 |
| 頂欄 🔍 按鈕 **右鍵** | 切換過濾器開關（臨時看原始 YouTube） |
| 影片卡片 hover | 底部滑出「🚫 隱藏」「⭐ 白名單」按鈕 |
| Tampermonkey 選單 | 提供「開啟面板」「切換過濾器」「立即備份設定」選項 |

### Debug 模式

透過 Tampermonkey 選單「🔍 切換 Debug 模式」開啟後：
- 瀏覽器 console 會輸出詳細運作日誌
- 可執行 `__ytfReport()` 查看統計
- 正常使用時**保持關閉**以避免雜訊

---

## 常見問題

### Q: Tampermonkey 沒偵測到新版本更新？
刪除舊版本 → 重新安裝新版本。安裝後透過面板右上角版本徽章確認載入的版本。

### Q: 匯出的 CSV 在 Excel 中中文亂碼？
確認版本為 **v1.1.0 以上**（已加入 UTF-8 BOM 修正）。若仍亂碼，嘗試 Excel「資料」→「從文字檔」選 UTF-8 編碼匯入。

### Q: 語言偵測不準？
拉丁語系語言（英文、西班牙文、越南文、法文等）因字元重疊偵測準確度較低，建議**搭配關鍵字過濾**做更精準的過濾。

### Q: 白名單加了還是被隱藏？
可能原因：
1. 頻道 ID 或名稱拼寫不一致（包含大小寫）
2. 頻道是合輯 / YouTube Mix（非單一頻道內容）
3. 如果是 Regex 模式，check pattern 是否正確比對

### Q: 為什麼有些影片標籤無法被關鍵字過濾？
關鍵字只比對**可見的標題與描述**。若影片的描述**被 YouTube 折疊或縮短**，描述比對可能失效。建議同時加入「標題」範圍。

### Q: 資料存在哪？
- `ytf_bl` / `ytf_wl` / `ytf_kg` / `ytf_fl` / `ytf_gl`：各類清單設定
- `ytf_st` / `ytf_hi`：統計與歷史
- `ytf_vcm`：videoId→channelId 快取
- `ytf_lang`：介面語言

所有資料儲存在 Tampermonkey 的 GM Storage 中，不上傳任何伺服器。

---

## 連結

- **GitHub**: https://github.com/hubig21/YouTube-Gatekeeper
- **贊助**：[Liberapay](https://liberapay.com/MayoHu_xu4fu/donate) ｜ [Ko-fi](https://ko-fi.com/L4L41XPRRX)
