# YouTube Channel Filter — User Guide v1.0.2

A powerful multi-layer YouTube video filter. Block channels, filter by keyword, duration, language, live streams and members-only content.

---

## 🚀 Installation

1. Install [Tampermonkey](https://www.tampermonkey.net/) (Firefox / Chrome / Edge)
2. Click the script install link and confirm in Tampermonkey
3. Refresh YouTube — the 🔍 icon appears in the top bar when active

---

## 🎛️ Basic Controls

| Action | Description |
|--------|-------------|
| 🔍 **Left-click** | Open / close the management panel |
| 🔍 **Right-click** | Toggle filter on / off instantly |
| Tampermonkey menu | Open panel or toggle filter from the extension icon |
| **Hover over a video card** | Quick action bar slides up: 🚫 Block channel ｜ ⭐ Whitelist |

---

## 🚫 Blocklist

Add channels to hide in the **🚫 Blocklist** tab:

- Enter a **channel name** (e.g. `Some Channel`) or **@handle** (e.g. `@channelhandle`)
- All videos from that channel disappear across the homepage, search results and sidebar recommendations
- Use **tags** to organise large blocklists
- Check **⌥R.* Regex mode** to match channel names with a Regular Expression pattern (see Regex section below)

---

## ⭐ Whitelist

Whitelisted channels **always appear**, regardless of any other filter:

- Highest priority — whitelist overrides blocklist, keyword, language and all other filters
- Useful when you enable broad keyword or language filters but want to protect specific channels
- Also supports **⌥R.* Regex mode** for pattern-based whitelisting

---

## 🔤 Keyword Filter

Create **keyword groups** to filter videos by title or description:

1. Click **＋ New Group** → enter a group name (e.g. `Sponsored`)
2. Set the scope: **Title**, **Description** or both
3. Add keywords — plain text or **Regex** supported
4. Toggle entire groups on / off to temporarily disable a topic

---

## ⚙️ Other Filter Settings

| Setting | Description |
|---------|-------------|
| ⏱ **Duration** | Set min / max seconds; videos outside the range are hidden (0 = unlimited) |
| 📡 **Live / Premiere** | Hide currently live streams / hide upcoming premiere videos |
| 👑 **Members Only** | Hide videos that require a paid membership |
| 🌐 **Language** | Auto-detect language from title characters; block or allow specific languages |

Language detection covers 20+ languages. You can also enter custom BCP-47 codes (e.g. `th`, `vi`, `id`).

Always click **💾 Save Settings** after making changes.

---

## ⌥R.* Regex Mode — Detailed Guide

Regular Expressions let a single pattern match many channel names or keywords at once.

### Format Rules

- **Enter the pattern directly** — no leading `/` or trailing `/i` needed
- **Case-insensitive by default** (equivalent to the `/i` flag always being on)
- Flag syntax is not supported — writing `memes/i` treats `/i` as part of the pattern

### Channel Name Regex (Blocklist / Whitelist)

Check **⌥R.* Regex mode** when adding a channel, then enter your pattern:

| Pattern | Effect |
|---------|--------|
| `memes` | Channel name contains `memes` **anywhere** (case-insensitive) |
| `^memes` | Channel name **starts with** `memes` |
| `memes$` | Channel name **ends with** `memes` |
| `\bmemes\b` | `memes` is a **standalone word** in the channel name |
| `^memes.*gaming` | Name starts with `memes` and also contains `gaming` |
| `meme(s)?` | Matches both `meme` and `memes` |
| `(shorts\|clips)` | Matches channel names containing `shorts` or `clips` |
| `^NASA$` | Exact full match for `NASA` (case-insensitive) |

> **Note:** Channel Regex matches the **channel name only**, not the @handle / channel ID. To filter by handle, use the exact @handle with Regex mode off.

### Keyword Regex (Keyword Groups)

Inside a keyword group, check **Regex** to match video titles or descriptions with a pattern:

| Pattern | Effect |
|---------|--------|
| `^\[SPONSORED\]` | Hide videos with a title starting with `[SPONSORED]` |
| `(buy one get one\|limited offer)` | Hide titles containing either phrase |
| `ep\.?\s*\d+` | Hide titles with episode numbers like `ep.1`, `ep 2`, `EP 3` |
| `https?://` | Hide videos whose description contains a URL |
| `\b(ad\|ads\|promo)\b` | Hide titles with standalone words `ad`, `ads` or `promo` |

### Common Pitfalls

- Special characters (`. * + ? ( ) [ ] { } ^ $ | \`) have special meanings in Regex. To match them literally, prefix with a backslash — e.g. `\[SPONSORED\]`
- An invalid pattern (mismatched parentheses, etc.) silently fails to match anything — no error is shown
- Test your patterns at [regex101.com](https://regex101.com) before adding them

---

## 💾 Export / Import

| Feature | Description |
|---------|-------------|
| **Full Settings JSON** | Backs up blocklist, whitelist, keywords and filter settings; import to restore on another device |
| **Individual CSV export** | Export blocklist / whitelist / keywords as CSV for spreadsheet editing, then re-import |
| **Stats JSON** | Export filter statistics separately (not included in full backup) |
| **History CSV** | Export the hidden-video log (title, channel, reason, time) |
| **Auto backup** | Set an interval in days; the script auto-downloads your settings JSON |
| **Share list** | Generate a link so others can import your filter settings directly |
| **Import mode** | Merge: keeps existing data and adds new entries; Overwrite: replaces everything |

> Stats and history are **not included** in the full settings backup. Use the **📊 Stats JSON** and **📋 History CSV** buttons to export them independently.

---

## ⚙️ Global Settings

Found at the bottom of the **⚙️ Settings** tab:

| Option | Description |
|--------|-------------|
| 🔔 **Show action notifications (Toast)** | When off, filter toasts are suppressed but still written to the panel log |
| 📊 **Show Stats & History tabs** | When off, the tabs are hidden; recording continues and export still works |

> Notifications triggered by the **quick action buttons** (🚫 / ⭐) on video cards **always appear**, even when Toast is disabled — so you always know the action completed.

---

## 🔍 Where Filtering Applies

Filters are active across all YouTube surfaces:

- **Homepage** recommendations
- **Search** results
- **Subscriptions** feed
- **Watch page** sidebar recommendations

---

## ❓ FAQ

**Q: Why don't some cards show quick action buttons?**
A: Buttons rely on the thumbnail being fully rendered. If the thumbnail is still lazy-loading, the bar may not appear or may be mispositioned. Let the page finish loading and hover over the card again.

**Q: Language filter isn't working?**
A: Detection is based on the **title's character set** — a video titled in English won't be caught even if the spoken language differs. Also make sure you clicked 💾 Save Settings.

**Q: My Regex pattern isn't matching anything?**
A: Common causes: (1) syntax error in pattern (e.g. unmatched parenthesis); (2) you included `/` or `/i` — remove them and enter only the pattern; (3) settings were not saved.

**Q: How do I export my statistics?**
A: Go to the **💾 Export/Import** tab and click **📊 Stats JSON**. Stats are not part of the full backup.

---

## Changelog

Current version: **v1.0.2**

See the full changelog below.

---

☕ If this script saves you time, consider supporting development via [Liberapay](https://liberapay.com/MayoHu_xu4fu/donate).
